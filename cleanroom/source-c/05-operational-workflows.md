# Source C — operational workflows

## Thresholds and alerts

**Threshold** tooling (per module tree) supports capacity or utilization warnings aligned with IP planning.

## Search and export

**Global search** with export paths; optional **OpenSearch** integration filenames suggest enterprise search add-ons.

## Request workflows

**IP request** style tools appear in the tree for self-service patterns (exact UX per upstream docs).

## Logs and audits

**Logs** viewers and export help operators trace changes—lighter than object-level changelog APIs of Source A but practical for SMB.
