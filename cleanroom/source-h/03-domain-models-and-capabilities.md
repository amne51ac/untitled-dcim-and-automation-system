# Source H — domain models and capabilities

## Core concepts

**Racks**, **objects** (devices), **ports**, **cables**, and **attributes**—minimal but coherent physical inventory.

## VLAN and switching

**802.1Q** include modules support VLAN port assignments—L2 documentation adjacent to racks.

## Load balancing helpers

**SLB** includes suggest optional **load balancer** object modeling—lighter than Source A’s dedicated load-balancer domain models.

## Device configuration

**deviceconfig** and **breed-*** includes hint at **vendor-specific** parsing helpers—operator aid, not full NMS.
